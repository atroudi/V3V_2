
class MotionEstimation:

    @staticmethod
    def estimateMotion(currentImage, nextImage, conversionParam, otherParam):
        """ 
            Computes motion vector for each pel
            Returns NULL in case of error
        """
        print("-- Motion Estimation")
        newImage = []
        return newImage
